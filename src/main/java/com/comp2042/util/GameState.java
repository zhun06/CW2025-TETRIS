package com.comp2042.util;

/** Represents the current state of the game*/
public enum GameState {
    START,
    PAUSE,
    RESUME,
    UPDATE,
    GAME_OVER,
    WIN,
    LOSE,
    RESTART,
    EXIT,
    LEADER_BOARD
}